spring:
  datasource:
    url: jdbc:mysql://192.168.25.144:3306/spring_cache?characterEncoding=utf-8
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

  redis:
    host: 192.168.25.144

mybatis:
  configuration:
    #开启驼峰命名法
    map-underscore-to-camel-case: true

#开启日志打印（包括sql语句）,logging.level.书写mapper类的包名,代码如下
logging:
  level:
    com:
      adu:
        springboot:
           mapper: debug


